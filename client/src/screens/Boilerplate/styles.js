import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container        : {
      flex           : 1,
      padding        : '16px',
      flexDirection  : 'row',
      flexWrap       : 'wrap',
    },
    body: {
      
    }
});

export default styles;
